spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ddd;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  h2:
    console:
      enabled: true # http://localhost:8080/h2-console でアクセス可能
mybatis:
  type-handlers-package: com.inkblogdb.ddd.infrastructure.database.typehandler
